org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official

# Kept in step with the parent build's gradle.properties. The point of this
# project is to resolve the *published* coordinates, so the versions are
# written out in full rather than inherited.
devkitGroup=io.github.ezekielwachira.devkit
devkitFillKitVersion=0.5.0
devkitNetKitVersion=0.3.0
devkitEcosystemVersion=0.1.0
